
services:
  api:
    build: .
    container_name: meta-prompt-generator-api
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app:ro
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
